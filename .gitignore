# 环境变量 / 密钥文件，禁止提交
.env
.env.*
*.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# 记忆存储（运行时生成的数据，禁止提交）
mem_store/
*.db
*.sqlite3
*.sqlite
facts.json
*_mem.json

# WorkBuddy 项目数据目录（含记忆/日志/设置，禁止提交）
.workbuddy/

# 审计日志（跑测试/诊断时生成，禁止提交）
audit.jsonl
*_audit.jsonl
